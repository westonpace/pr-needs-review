* When a PR is created that is ready for review it should have the awaiting-review label
* When a PR is created that is draft it should not have the awaiting-review label
* When a PR is converted from draft to ready for review it should get one of the labels
  * Test each case
* When a PR is converted from ready for review to draft both labels should be removed.
  * But not other labels
